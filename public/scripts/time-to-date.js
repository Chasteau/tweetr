$(document).ready(function(){
  // console.log("I am working");
  $( "span.time-to-date" ).##event?(function(event ) {

    // attach an event handler

    // collect the value of text node

    // convert text to number

    // convert that number which is in seconds to time

    // append to string `date created ${date}`


    let textInput = this.value;
      counter -= textInput.length;



       // if counter is less than 5 warn user with red css
       if(counter <= 3) {
        $(this).nextAll(".counter")
        .css("color", "red");
       } else {
        $(this).nextAll(".counter")
        .css("color", "black");
      }
  });
});